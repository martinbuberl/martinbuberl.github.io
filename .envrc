# Fixing " GitHub Metadata: No GitHub API authentication could be found." warning
# Add "JEKYLL_GITHUB_TOKEN=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX" to .env
while read -r line; do export $line; done < .env
